// here, we will declare our constant variables
const PORT = 3100;
const APIPATH = '/api/v1/';  // this can be any string, a starting path for the API

// now, we will export all constant variables
module.exports = { PORT, APIPATH };